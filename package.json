{
  "name": "line-talent-assistant-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "LINE Talent Inquiry Assistant MVP for auto-replies, escalation, and manager sentence drafts.",
  "main": "index.js",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "simulate:webhook": "node scripts/simulate-webhook.js",
    "retention:cleanup": "node scripts/cleanup-retention.js"
  },
  "engines": {
    "node": ">=18"
  }
}
